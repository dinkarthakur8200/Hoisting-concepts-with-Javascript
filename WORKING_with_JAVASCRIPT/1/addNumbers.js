// Call the function before it's declared
console.log(addNumbers(5, 3));  // This will output 8

// Function declaration
function addNumbers(a, b) {
    return a + b;
}
// In this code:

// We call addNumbers(5, 3) before the function is declared.
// The function addNumbers is then declared, taking two parameters a and b.
// The function returns the sum of a and b.